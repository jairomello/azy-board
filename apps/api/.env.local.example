# PostgreSQL local — copie para .env.local e ajuste.
# Exige o container pg-local rodando (~/Projetos/pg-local/manage.sh start).
#
# Para usar SQLite (padrão, sem serviços externos), não defina DATABASE_URL.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/azyboard