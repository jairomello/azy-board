# /azyboard-update

Confirme o item e registre progresso relevante com `update_item`, checklist ou `create_item_log`. Use `move_task` com o nome exato da coluna somente em item folha. Não repita falhas não-retryable.
