# /azyboard-status

Consulte o contexto autorizado com `list_projects`, `get_project`, `get_board` e, quando útil, `get_current_sprint` e `list_tasks`. Resuma o estado sem alterar dados. Se o projeto não estiver claro, peça o nome ou liste opções acessíveis.
